# PCMCI验证完整指南（零基础版）

## 📚 目录
1. [什么是PCMCI？](#什么是pcmci)
2. [代码在做什么？](#代码在做什么)
3. [运行代码](#运行代码)
4. [结果解读](#结果解读)
5. [后续研究怎么开展](#后续研究怎么开展)

---

## 什么是PCMCI？

### 简单理解
**PCMCI**（PC-Maximum Conditional Independence）是一种**时间序列因果发现方法**。

### 类比理解
想象你观察社交媒体上的事件传播：
- **问题**：情感分数高 → 传播量大，这是真的因果关系吗？
- **PCMCI的作用**：帮你找出**真正的因果关系**，排除虚假的相关性

### PCMCI能做什么？
1. **发现因果关系**：哪些变量影响哪些变量
2. **识别滞后**：原因影响结果需要多长时间
3. **构建因果图**：可视化变量之间的因果关系

### 为什么用PCMCI？
- ✅ 专门为**时间序列数据**设计
- ✅ 能处理**非线性关系**
- ✅ 能处理**混杂因素**（虽然不完美）
- ✅ 有成熟的Python库（tigramite）

---

## 代码在做什么？

### 整体流程

```
加载数据 → 运行PCMCI → 分析结果 → 可视化 → 保存结果
```

### 详细步骤

#### 步骤1: 加载数据 (`load_b1c_data`)
**在做什么？**
- 读取CSV文件
- 提取观测变量（X1, X2, X3...）
- **丢弃U列**（混杂变量，你看不到）
- **丢弃time列**（PCMCI会自动处理时间顺序）

**为什么这样做？**
- 模拟真实情况：你只能看到X1-X6，看不到U
- 测试PCMCI能否在有混杂的情况下发现真实因果关系

#### 步骤2: 运行PCMCI (`run_pcmci`)
**在做什么？**
1. **创建数据处理器**：将数据转换为PCMCI能理解的格式
2. **选择检验方法**：ParCorr（偏相关检验）
3. **运行PCMCI算法**：
   - 测试变量之间的条件独立性
   - 识别显著的因果关系
   - 估计因果强度

**参数说明：**
- `tau_max=3`：最大滞后（原因最多在3个时间单位前影响结果）
- `alpha_level=0.05`：显著性水平（95%置信度）

#### 步骤3: 分析结果 (`print_causal_relationships`)
**在做什么？**
- 找出所有显著的因果关系
- 显示：谁影响谁、滞后多少、相关性多强、p值多少

**输出示例：**
```
1. X1 --[2步滞后]--> X2
   相关性: 0.5234
   p值: 0.001234
```

#### 步骤4: 可视化 (`visualize_results`)
**在做什么？**
- 生成**因果图**：箭头表示因果关系
- 生成**相关性热力图**：颜色深浅表示相关性强弱

**为什么重要？**
- 直观理解变量间的关系
- 方便在论文中使用

#### 步骤5: 保存结果 (`save_results_to_file`)
**在做什么？**
- 将所有结果保存到CSV文件
- 包含：所有变量对、滞后、相关性、p值、是否显著

**为什么重要？**
- 后续分析需要
- 论文写作需要数据支撑

---

## 运行代码

### 第一步：安装依赖

```bash
# 安装tigramite（PCMCI的Python库）
pip install tigramite

# 安装其他依赖（如果还没有）
pip install pandas numpy matplotlib seaborn
```

### 第二步：检查数据路径

确保你的B1C数据在正确的位置：
```
data/TimeGraph/B1C/Gaussian error/6 variable/Lag 3/nonlinear_confounded_n3000_vars6_lag3_gaussian.csv
```

如果文件不存在，代码会自动尝试其他路径。

### 第三步：运行代码

```bash
python pcmci_validation.py
```

### 第四步：查看结果

运行完成后，会在 `results/pcmci_validation/` 目录下生成：
1. **pcmci_causal_graph.png** - 因果图
2. **pcmci_results.csv** - 详细结果

---

## 结果解读

### 1. 控制台输出

#### 数据加载信息
```
📂 正在加载数据: ...
   数据形状: (3001, 8)
   列名: ['X1', 'X2', 'X3', 'X4', 'X5', 'X6', 'U', 'time']
   ✅ 成功加载 6 个观测变量: ['X1', 'X2', 'X3', 'X4', 'X5', 'X6']
   ✅ 数据维度: (3001, 6) (时间点数 × 变量数)
```
**含义**：
- 成功加载了3001个时间点的数据
- 有6个观测变量（X1-X6）
- U和time列已被丢弃

#### PCMCI运行信息
```
🔍 开始运行PCMCI因果发现...
   最大滞后 (tau_max): 3
   显著性水平 (alpha): 0.05
   正在计算因果关系...
   ✅ PCMCI运行完成！
```
**含义**：
- 正在分析因果关系
- 考虑最多3个时间单位的滞后
- 使用95%置信度

#### 发现的因果关系
```
📋 发现的因果关系 (显著性水平: 0.05):
================================================================================
   ✅ 发现 8 个显著的因果关系:

   1. X1 --[2步滞后]--> X2
      相关性: 0.5234
      p值: 0.001234

   2. X3 --[1步滞后]--> X4
      相关性: 0.4123
      p值: 0.002345
   ...
```
**含义**：
- **X1影响X2**：X1在2个时间单位前影响X2
- **相关性0.5234**：中等强度的正相关
- **p值0.001234**：非常显著（<0.05）

### 2. 因果图（pcmci_causal_graph.png）

#### 左图：因果图
- **节点**：每个变量（X1, X2, X3...）
- **箭头**：因果关系（X1 → X2 表示X1影响X2）
- **颜色**：因果强度（颜色越深，影响越强）
- **数字**：滞后时间（1, 2, 3...）

#### 右图：相关性热力图
- **颜色深浅**：相关性强弱
- **数值**：平均相关性
- **对角线**：变量与自己的相关性（总是1）

### 3. 结果CSV文件（pcmci_results.csv）

**列说明：**
- **From**：原因变量
- **To**：结果变量
- **Lag**：滞后时间
- **Correlation**：相关性（-1到1）
- **P_Value**：p值（越小越显著）
- **Significant**：是否显著（True/False）

**如何使用：**
- 筛选显著关系：`Significant == True`
- 按相关性排序：找出最强的因果关系
- 分析滞后模式：哪些关系是即时的，哪些是延迟的

---

## 后续研究怎么开展

### 阶段1：理解PCMCI结果（当前阶段）

#### 任务1：运行代码，查看结果
- ✅ 运行 `pcmci_validation.py`
- ✅ 查看生成的因果图
- ✅ 理解发现的因果关系

#### 任务2：对比不同配置
**实验设计：**
1. **不同变量数**：4变量 vs 6变量 vs 8变量
2. **不同滞后**：Lag 2 vs Lag 3 vs Lag 4
3. **不同样本量**：n1000 vs n3000 vs n5000
4. **不同噪声**：Gaussian vs Students t

**目的：**
- 理解各参数对结果的影响
- 找到最适合的参数设置

#### 任务3：评估准确性（如果有ground truth）
**如果有ground truth图（_graph.png）：**
- 对比PCMCI发现的因果关系
- 计算准确率、精确率、召回率

**评估指标：**
- **准确率**：发现的因果关系中，有多少是正确的
- **召回率**：真实的因果关系中，有多少被发现了
- **F1分数**：准确率和召回率的综合指标

### 阶段2：应用到你的抖音数据

#### 任务1：数据预处理
**需要做什么：**
1. **加载抖音数据**：读取JSON文件
2. **情感分析**：对评论内容进行情感分析
3. **时间聚合**：按小时聚合数据
4. **构建时间序列**：
   - 每小时评论数
   - 每小时平均情感分数
   - 每小时总点赞数
   - 每小时总分享数
   - 每小时新视频数

**代码框架：**
```python
# 1. 加载数据
comments = load_douyin_comments('data/douyin/search_comments_2025-12-03.json')

# 2. 情感分析
comments['sentiment'] = analyze_sentiment(comments['content'])

# 3. 按小时聚合
hourly_data = comments.groupby('hour').agg({
    'sentiment': 'mean',
    'like_count': 'sum',
    'comment_id': 'count',
    # ...
})

# 4. 构建时间序列
time_series = hourly_data[['sentiment', 'like_count', ...]].values
```

#### 任务2：运行PCMCI
**使用处理好的抖音数据：**
```python
# 运行PCMCI
results, pcmci = run_pcmci(time_series, var_names=['情感', '评论数', '点赞数', ...])

# 可视化
visualize_results(pcmci, results, var_names, tau_max=3)
```

#### 任务3：解释结果
**需要回答的问题：**
1. **情感 → 传播量**：正面情感是否真的导致高传播？
2. **早期传播 → 后期传播**：是否存在正反馈环？
3. **官方干预 → 传播量**：官方干预是否真的降低了传播？

### 阶段3：效应估计（DoubleML + RDD）

#### 任务1：定义处理变量
**例子：官方干预**
- **处理变量**：是否有官方公告（0/1）
- **结果变量**：传播量（评论数、点赞数等）
- **协变量**：时间、地理位置等

#### 任务2：使用DoubleML估计效应
**目的：**
- 量化"官方干预对传播量的影响"
- 给出置信区间

#### 任务3：使用RDD验证
**目的：**
- 在官方干预的时间点前后，传播量是否有显著变化
- 验证因果关系的稳健性

### 阶段4：预测模型（LSTM）

#### 任务1：构建LSTM模型
**输入：**
- 历史时间序列数据
- PCMCI发现的因果特征

**输出：**
- 未来传播量预测

#### 任务2：对比实验
**对比模型：**
- ARIMA（传统时间序列模型）
- 普通LSTM（不使用因果特征）
- **因果特征增强LSTM**（你的创新点）

**评估指标：**
- MAE（平均绝对误差）
- RMSE（均方根误差）
- MAPE（平均绝对百分比误差）

#### 任务3：证明优势
**目标：**
- 证明你的模型比基准模型好15%以上
- 分析为什么更好（因果特征的作用）

### 阶段5：论文写作

#### 第3章：数据与方法
- 3.3 因果发现：展示PCMCI结果
- 3.4 因果图精炼：基于PCMCI结果构建SCM
- 3.5 效应估计：DoubleML + RDD结果
- 3.6 预测模型：LSTM结果

#### 第4章：实证分析
- 4.2 因果图呈现：PCMCI发现的因果图
- 4.3 关键因果效应量化：具体的数字和置信区间
- 4.4 传播预测对比实验：LSTM vs 基准模型
- 4.5 机制讨论：正反馈环、官方断路器

---

## 常见问题

### Q1: PCMCI运行很慢怎么办？
**A:**
- 先用小数据集测试（n500, 4变量）
- 减少tau_max（从3降到2）
- 使用更快的独立性检验方法

### Q2: 没有发现显著的因果关系？
**A:**
- 检查数据是否正确加载
- 尝试调整alpha_level（比如0.1）
- 检查数据中是否真的有因果关系
- 尝试不同的独立性检验方法（GPDC用于非线性）

### Q3: 结果和预期不一致？
**A:**
- 这是正常的！PCMCI是数据驱动的方法
- 对比ground truth（如果有）
- 分析可能的原因（混杂因素、样本量不足等）
- 在论文中讨论这些发现

### Q4: 如何选择参数？
**A:**
- **tau_max**：从数据集的Lag信息获取（Lag 3 → tau_max=3）
- **alpha_level**：通常用0.05（95%置信度）
- **独立性检验**：线性用ParCorr，非线性用GPDC或CMIknn

### Q5: 如何评估结果好坏？
**A:**
- **有ground truth**：计算准确率、精确率、召回率
- **无ground truth**：看结果是否合理、是否符合领域知识
- **鲁棒性**：不同参数下结果是否一致

---

## 📝 总结

### 你现在应该做什么？

1. **立即行动**：
   - ✅ 安装tigramite：`pip install tigramite`
   - ✅ 运行 `pcmci_validation.py`
   - ✅ 查看生成的结果

2. **理解结果**：
   - ✅ 看懂因果图
   - ✅ 理解发现的因果关系
   - ✅ 分析结果是否合理

3. **下一步**：
   - ✅ 尝试不同的参数配置
   - ✅ 对比不同数据集的结果
   - ✅ 准备应用到你的抖音数据

### 关键文件

- **pcmci_validation.py** - 主代码文件
- **PCMCI验证完整指南.md** - 本文件（详细说明）
- **results/pcmci_validation/** - 结果文件夹

### 需要帮助？

如果遇到问题：
1. 检查错误信息
2. 查看本文档的"常见问题"部分
3. 检查数据路径是否正确
4. 确认tigramite已正确安装

---

**祝你毕设顺利！** 🎉

